apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-lab-production-tls
  namespace: production
spec:
  secretName: k8s-lab-production-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
  - production.k8s-lab.local